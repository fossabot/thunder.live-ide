{
  "extends": ["../../.eslintrc.json"],
  "ignorePatterns": ["!src/**/*"],
  "overrides": [
    {
      "files": ["*.ts"],
      "parserOptions": { "project": ["libs/eslint/tsconfig.*?.json"] }
    }
  ]
}
